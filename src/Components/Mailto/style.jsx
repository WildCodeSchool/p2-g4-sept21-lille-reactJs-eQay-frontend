import Styled from 'styled-components';

const SMailto = Styled.div`
a {
  text-decoration: none;
  color: var(--color-secondary);
}
a:hover
{
  color: var(--color-primary)
}
`;

export default SMailto;
